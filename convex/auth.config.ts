// https://champion-mudfish-36.clerk.accounts.dev

const authConfig = {
    providers: [
        {
            domain: "https://champion-mudfish-36.clerk.accounts.dev",
            applicationID: "convex"
        },
    ]
};

export default authConfig;